# Session 2026-06-20 (Saturday) — "Eco Omakase" class: workshop, build, ship (54 → 57)

System date 2026-06-20. Class audio device-stamped 2026-06-20 (DJI_34–38, ~98 min).

## Arc of the session
Alex asked what to focus on in class to round out the system → I pointed at the two biggest gaps (submission/finishing offense, typed center clinch) + kick-defense. He recorded the class, brought back 5 audio files, and we ran the full loop: transcribe → mine → workshop → eco-audit → build → verify → commit.

## Accomplishments
- **Transcribed** the class on-device (mlx_whisper large-v3, chunked + foreground), 10,173 words. Mining folded into the workshop doc.
- **Workshopped + eco-audited** all 5 games + 2 doctrines against the 20-rule set (`ECOLOGICAL_PSYCHOLOGY_AUDIT.md` §4) and `eco-check.py`. Findings in `docs/_drafts/class-workshop-2026-06-20.md`.
- **Built 3 new games** (all WIP, pending mat re-run), each closing a zero-coverage gap:
  - `mount-submission-hunt` — first **submission-offense** game (gap #1). Finish layer of the mount ladder (escape→maintain→hunt). Win = arm over the head, hands locked (bicep to ear), 1-2s; OR back sealed on top 1-2s. Bottom wins by **deny + recover one leg** (fixes the fatigue stalemate Alex flagged). Shown strike opens the frame.
  - `fifty-fifty-clinch` — first **typed center-clinch** game (gap #2). Neutral over-under, diagonally matched (NOT "mirror"), leg connection above the knee, **strikes = the anti-stall constraint**, off-balance scores only if you stay standing.
  - `kick-defense` — first **kick-defense isolation** game (gap #5). Names **τ** as the read; mastery level re-introduces the head-kick threat to calibrate the catch's real risk (resolves the nerf tension Alex spotted); kicker stays adversarial (task-simplified, loop intact).
- **Refined** `body-lock-to-ground` from today's **R5 mat re-validation**: defender now breaks back to the 50/50 clinch (links to the new game), wall-proximity start, pummel-incentive note.
- **New concept page** `finishing-sequence` — the doctrine, **eco-rewritten** (calm = attunement, not central-executive "calculate"); credits John McCarthy. Bull/Soldier/Matador already lived in `winning-the-circle`, so no duplicate.
- **Verified**: eco-check 56/57 clean (only pre-existing `turtle-breakout`), `mkdocs build --strict` green, `consistency-check` clean except the known home-links failure, `games.json` regenerated to 57.
- **Two HTML briefings** in `explainers/status-review-2026-06-20.html` (overwritten same-day: first "what I learned," then the build review with expandable game cards + before/after on the mount fixes).

## Key decisions
- **Mount reframe APPROVED** by Alex: Mount Submission Hunt = the finish layer of the mount ladder, with deny-and-recover bottom win + short rounds.
- **Finishing Sequence doctrine** has the one genuine eco conflict of the class ("calm then calculate" smuggles back the central executive). Rewrote it as attunement + phase transition + afforded finish.
- 50/50 clinch placed in nav under a new `Open Space → Wall: Center Clinch` subsection (signals the new category; clinch IA may deserve its own environment later).
- New games stay **WIP** until Alex re-runs them on the mat (R5).

## Corrections / lessons
- **Transcription: background gets KILLED, big files OOM.** A `nohup … &` chunk loop dies right after model load (harness reaps detached process on tool return; reports "exit 0" with no output). Big single files (~98 min) silently OOM with the same signature. Fix: **chunk to ≤16 min + run FOREGROUND**, verify word counts at each step. Updated `reference_transcription_pipeline.md` (superseded the old "run in background" advice).
- Alex's working preference reinforced: **build + verify empirically + show work (html) BEFORE committing.** Aligns with don't-oversell + visual-is-king.

## Files changed (commit 45db7e6)
New: `docs/games/{fifty-fifty-clinch,mount-submission-hunt,kick-defense}.md`, `docs/concepts/finishing-sequence.md`, `docs/_drafts/class-workshop-2026-06-20.md`, `explainers/status-review-2026-06-20.html`.
Modified: `docs/games/body-lock-to-ground.md`, `docs/games/index.md`, `docs/system/map.md`, `docs/assets/data/games.json`, `mkdocs.yml`.

## Blockers / open items
- **Not pushed**: branch `ground-wave-guard-2026-06-06` is **3 ahead of origin** (today's build + 2 prior unpushed commits). PR strategy still open from 6/14.
- Graduate `body-lock-to-ground` from WIP? (R5 done today — Alex's call.)
- **Game 5 (Wall Dirty-Boxing)** — fills the integration gap but was only coached in sparring; needs a mat run before building.
- **"Shown strike / legitimate threat without violence"** — load-bearing in 3+ games; formalize as a named glossary house rule + the finger-curl safety note.
- New games + concept need **hero images** (claude.ai Higgsfield, ember-silhouette, 16:9). Missing PNGs don't break the build.
- Pre-existing `turtle-breakout` eco-check E1 (missing perimeter) — trivial fix still outstanding.

## Next session pickup
Decide push/PR strategy, then either start the **submission-offense wave** (RNC from back, triangle/armbar from guard, kimura from side/NS — same ladder model as Mount Submission Hunt) or formalize the shown-strike house rule + mat-test Game 5. See `memory/NEXT-SESSION.md`.
