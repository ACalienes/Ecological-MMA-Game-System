# Session 2026-08-23

Friday 2026-08-21's class pulled off the mic, mined, built and deployed the same day. Library 62 → 64.

## Accomplishments

- **Recovered the audio.** Located Friday's class on the DJI mic, verified it was the right recording by transcribing four sampled clips before trusting the filenames, and pulled both files md5-verified. 60:44 unbroken, 6,436-word transcript, plus a timestamped version.
- **Mined it.** `docs/_drafts/class-mining-2026-08-21.md`, ~2,950 words. Alex answered all 8 open questions via an in-page ballot.
- **Built the wave.** Two games (`closing-the-distance`, `clinch-holder`), one concept (`three-points-of-pressure`), three ember heroes, 50/50 Clinch reworked, neck safety rule across all 8 wall pages.
- **Fixed the broken toolchain.** The `.venv` shebang still pointed at `/Users/alex/Desktop/Code/...`, dead since the iCloud migration, which blocked every build and deploy. Rebuilt on Python 3.14 with the original pins and proved it by deploying through it.
- **Deployed and verified live.** gh-pages `6bac4fa`, 200s confirmed on every new page and image.

## Decisions (Alex, this session)

1. Build both games, Closing the Distance first.
2. The redirect win does **not** generalise yet. Live in the new game, flagged pending a mat test, deliberately not backported.
3. Closer/Spacer and Holder/Escaper on the new pages only, not backported across the clinch family.
4. **"Damage while holding" is the house anti-stall standard**, expressed through the win condition rather than a bolted-on strike tax. Applied to 50/50 Clinch as well as the new page.
5. Three Points of Pressure gets its own concept page.
6. Rework 50/50 Clinch alongside the new sibling rather than blessing it as-is.
7. Neck safety note on every wall page, not only pinning-win pages.
8. The Muay Thai opinion goes on the site in softened form.
9. Rebuild the site tools, and pull the 43 old mic recordings.

## Files changed

`docs/_drafts/class-mining-2026-08-21.md` · `docs/games/closing-the-distance.md` · `docs/games/clinch-holder.md` · `docs/concepts/three-points-of-pressure.md` · `docs/games/fifty-fifty-clinch.md` · `docs/games/{wall-escape,wall-defensive-submission,wall-control,wall-grinding,wall-to-ground,standup-loop,pressure-to-wall}.md` · `docs/games/index.md` · `docs/system/map.md` · `docs/reference/prerequisites.md` · `docs/assets/data/games.json` · `mkdocs.yml` · `overrides/home.html` · `explainers/status-review-2026-08-23.html` · `memory/NEXT-SESSION.md` · 3 heroes in `docs/assets/img/heroes/` · `.venv` rebuilt

## Errors encountered

- **`cp` fails FAT32 to APFS** on this card: `fcopyfile failed: Invalid argument`. `rsync -a --partial --inplace` works and resumes.
- **The mic auto-unmounts mid-copy** and will not re-enumerate without a physical replug. Cost two attempts.
- **A false-pass in my own verification**: comparing two unreadable md5s returned "equal" and printed OK for a file that had not copied. Verification now guards against empty checksums.
- **`overrides/home.html` had silently drifted to 60 games.** `consistency-check.py` does not cover that file, so nothing caught it. Now 64 and noted in NEXT-SESSION.
- **26 em dashes written into the mining report** before being caught by diffing against the 8/14 report. See re-explanation debt.

## Blockers

**The 43 never-offloaded mic recordings**, including 7/27 and 7/29 sessions not in this repo. Alex authorised the pull; the mic powered itself down and needs a physical replug. `~/Code/ACD/scripts/ops/dji_mic_offload.py` cannot detect this older transmitter's folder layout, so it must be done by hand.

## Next session pickup

Replug the mic and pull the 43 files. Then, once Alex has run the two new games on the mat, the blessing pass, and specifically whether the redirect win generalises. Full pickup prompt in `memory/NEXT-SESSION.md`.
