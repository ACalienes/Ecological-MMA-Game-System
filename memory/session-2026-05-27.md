# Session 2026-05-27

## Accomplishments

**Discord harvest (both batches captured + pressure-tested):**
- Saved Batch 1 (forums) + Batch 2 (game-design-and-review channel + K-Guard thread, ~11 months) to `docs/_drafts/discord-harvest-2026-05-26.md`. Neutralized, names for traceability only, branded framings stripped.
- Batch 2 **closed gaps I had wrongly said the source couldn't fill**: per-position mount/turtle/KOB offense + escape, back maintenance with hook discipline, K-guard (a position we don't cover at all), leg-lock + guillotine-family entries.
- Extracted portable meta-frameworks worth more than single games: **sub-control framework** (breanne), **no-C2C control**, **continuous-vs-terminal asymmetry**, **secret-missions deck** (a product feature), **disc/marker objective**.
- **Pressure-tested every distinct new game** against our QA rubric → `docs/_drafts/harvest-pressure-test-2026-05-27.md`. Result: ~35 PASS, ~15 CONDITIONAL, ~15 NOT-YET. Headline: "SOLID" tracked "clear idea," not "meets our observable-win bar." Dominant defect = the same "hold as long as possible / no threshold" flaw qa-findings caught in the existing library.

**First per-position ground games BUILT (the real shift this session):**
- `docs/games/mount-escape.md` — Aaron's 5-rung frame ladder. The exemplar that locked the template + the threshold house rule.
- `docs/games/side-control-escape.md` — RobDurden's stepped-top-control ladder (control ladder flexed the other direction: top's pin increases per level).
- `docs/concepts/guard-recovery.md` — new shared concept. Guard recovery = graded ladder of how much leg you reinsert across the hip/knee/foot line (one foot < one knee/half guard < both knees < both feet < closed loop). Mirrors the passing line-of-feet/knees/hips language. Floor for a recovered guard = half guard.

**Standardization:** mount-escape, side-control-escape, ground-escape, leg-reclaim all now link the guard-recovery concept. One definition, no drift.

**QA findings audited (HIGH-objective only):** tight-block body-kick tag = VALID; standup-loop 3-starts = VALID but milder; ground-access positional-battle prereq = VALID; **winning-the-circle links = FALSE ALARM** (markdown .md links resolve fine; no macros plugin renders frontmatter; strict build confirms). MED/LOW unaudited.

## Decisions
- **Threshold house rule:** every maintenance/continuous game must resolve on a stated time or count (start 20s), never "as long as possible." Bake into the page template.
- **Per-position game template = `mount-escape.md`.** Built on the `ground-escape.md` shape; the control-ladder is the standard Levels structure.
- **Guard recovery is a GRADED win (Option A), floor at half guard (one knee in), defined once as a shared concept page (scope option 2)** — Alex's calls.
- **Top's win = an observable advance/isolation, not a stall:** mount → isolate an arm; side control → advance to mount/back.

## Files changed
- New: `docs/games/mount-escape.md`, `docs/games/side-control-escape.md`, `docs/concepts/guard-recovery.md`
- New drafts: `docs/_drafts/discord-harvest-2026-05-26.md`, `docs/_drafts/harvest-pressure-test-2026-05-27.md`
- New root HTML explainers: `qa-audit-explainer-2026-05-26.html`, `guard-recovery-options-2026-05-27.html`
- Edited: `mkdocs.yml` (nav: 2 games + 1 concept), `docs/games/index.md`, `docs/games/ground-escape.md`, `docs/games/leg-reclaim.md`
- Toolchain note: `mkdocs` runs via `uvx --with mkdocs-material mkdocs build --strict`. Dev server on :8801 via uv. All builds this session = EXIT 0, zero warnings.

## Blockers / outstanding
- **3 hero images missing:** `mount-escape.png`, `side-control-escape.png`, `guard-recovery.png`. Pages complete; banners broken until generated (use the image pipeline to match existing hero style).
- **2 confirmed QA objective bugs still NOT fixed** (I said I'd fold them in, didn't): `tight-block` remove `body-kick` tag; `ground-access` remove `positional-battle` prereq.
- **4 QA domain calls still pending Alex:** 50/50 "mirror grips" wording (3 files incl. winning-the-circle:37); evade-the-punch duck-vs-roll; standup-loop canonical start; ground-access replacement prereqs.
- **Stale:** `.claude/worktrees/` has 3 untracked worktrees (agitated-robinson, inspiring-einstein, loving-nobel) — not from this session, likely abandoned; worth cleaning.
- Nothing committed this session — all on `main`, working tree dirty (Alex declined/wasn't asked to commit yet).

## Next session pickup
Escape family is 2 of ~5 built (mount, side-control) on a proven template + shared concept. Next obvious build: **Back Escape** (RNC race / hand-fighting), then Half-Guard recover/sweep. Or clear the debt first: generate the 3 hero images, fix the 2 QA bugs, commit.
